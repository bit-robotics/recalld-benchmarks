diff --git a/src/memory_bench/dataset/locomo.py b/src/memory_bench/dataset/locomo.py
index 535985d..73e38c6 100644
--- a/src/memory_bench/dataset/locomo.py
+++ b/src/memory_bench/dataset/locomo.py
@@ -93,11 +93,14 @@ class LoComoDataset(Dataset):
 
     @staticmethod
     def _session_keys(conv: dict) -> list[str]:
-        """Return sorted session keys (e.g. ['session_1', 'session_2', ...])."""
+        """Return session keys in their numeric, chronological order."""
         return sorted(
-            k for k in conv
-            if k.startswith("session_") and not k.endswith("_date_time")
-            and isinstance(conv[k], list)
+            (
+                k for k in conv
+                if k.startswith("session_") and not k.endswith("_date_time")
+                and isinstance(conv[k], list)
+            ),
+            key=lambda key: int(key.removeprefix("session_")),
         )
 
     @staticmethod
@@ -284,6 +287,7 @@ If it's correct, set correct=true.
         category: str | None = None,
         limit: int | None = None,
         ids: set[str] | None = None,
+        user_ids: set[str] | None = None,
     ) -> list[Document]:
         data = self._load_raw()
         documents: list[Document] = []
@@ -293,6 +297,8 @@ If it's correct, set correct=true.
 
         for item in data:
             sample_id = item["sample_id"]
+            if user_ids is not None and sample_id not in user_ids:
+                continue
             if conv_filter is not None and sample_id != conv_filter:
                 continue
             conv = item["conversation"]
